apiVersion: v1
kind: Service
metadata:
  name: {{ include "edukan-productservice-chart.fullname" . }}
  labels:
    {{- include "edukan-productservice-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "edukan-productservice-chart.selectorLabels" . | nindent 4 }}
